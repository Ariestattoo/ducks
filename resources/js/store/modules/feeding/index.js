import getters from './getters'
import actions from './actions'
import mutations from './mutations'

const state = {
  foods: null,
  feedings: null,
  pending: null,
  
}

export default {
  state,
  getters,
  actions,
  mutations
}
